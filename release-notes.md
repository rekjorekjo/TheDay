1. 首页布局调整：底部导航保留“日子、分类、新增、设置”，首页右上角改为工具栏入口。
2. 工具栏新增“导出、里程碑、计算器”三个功能入口，工具栏页面支持返回，功能卡片仅显示图标和名称。
3. 导出功能移入工具栏，导出页增加返回按钮。
4. 新增里程碑功能：显示当前年份和月份进度，百分比保留两位小数；里程碑仅在该页面可见。
5. 里程碑列表支持长按拖动排序并保存顺序；长按卡片进入管理态并高亮当前项，但不会自动勾选。
6. 里程碑管理态底部悬浮工具栏提供删除、导出、完成三个纯图标按钮；单卡片右侧不再显示删除按钮。
7. 里程碑导出会先进入排序页，支持长按拖动调整导出顺序，再进入装饰主题页分享或保存。
8. 新增日期计算器：支持按天、周、月、年计算某日期之前或之后的日期，也支持计算两个日期间隔。
9. 日期计算器布局优化：数字与单位在同一行，之前/之后单独放在下一行左侧。
10. “新增”恢复为底部导航主页面，不再显示返回按钮，并保留底部导航栏。
11. 应用中可见的删除、清空、移除类操作使用更醒目的错误色系。
12. 更新 README 和应用内使用说明。
