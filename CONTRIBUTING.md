# Contributing

欢迎提交 Issue 与 Pull Request。请遵循以下原则：

1. 核心用户数据必须保持本地，不引入广告、统计、追踪、账号或云同步；新增网络功能必须具有明确用途，并同步更新隐私说明。
2. 新依赖应说明必要性，并优先使用 Android 官方组件。
3. UI 不复制其他倒数日应用的版式、图标或品牌资产。
4. 核心日期算法应补充单元测试。
5. 提交前运行 `./gradlew test` 和 `./gradlew assembleDebug`。
